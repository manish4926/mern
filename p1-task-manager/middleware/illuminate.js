// This middleware is used to check multiple things like route available, auth header,

const illuminateIdentifier = () => {
    // return res.status(500).json({ msg: error });
}

module.exports = illuminateIdentifier;